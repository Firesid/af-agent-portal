FROM python:3.12-slim

WORKDIR /app
COPY scripts/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY scripts/build_data.py .

ENTRYPOINT ["python", "build_data.py", "--bucket", "af-agent-portal-data", "--report"]
